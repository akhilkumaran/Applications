Access the application by http://localhost:8080/application/ . URL may change depending upon webserver configuration

This is a simple single module application which will not use any security features. We can add security features like, password check and all.
We may need to mask many things like account number while displaying and all.

As of now application is not checking duplicate user, and providing any options for updating their info